{
  services.mako = {
    enable = true;
    settings = {
      maxVisible = -1;
      layer = "overlay";
      anchor = "top-right";
      defaultTimeout = 5000;
      ignoreTimeout = false;
    };
  };

} # EOF
# vim: set ts=2 sw=2 et ai list nu
